import { generateToken } from "../lib/utils.js";
import Student from "../models/student.js";
import bcrypt from 'bcryptjs'
import cloud from "../lib/cloudinary.js";

export const questionRead = async (req,res) =>{
    
}